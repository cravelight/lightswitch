<?php

use Illuminate\Database\Capsule\Manager as Capsule;

/**
 * ...
 */
class {{classname}} {
    public function run(Capsule $db)
    {
//        $db->schema()->dropIfExists('users');
//        $db->schema()->create('users', function($table) {
//            $table->increments('id');
//            $table->string('username');
//            $table->timestamps();
//        });
    }
}